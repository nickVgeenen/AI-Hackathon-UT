// src/components/TransitionCenter.jsx
import React, { useState } from 'react';

const TransitionCenter = () => {





    //what would be in just file is just created in the transition interface file

    return (
        <label>hoi</label>
    );
};

export default TransitionCenter;